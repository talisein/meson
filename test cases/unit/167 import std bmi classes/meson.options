# The divergent consumer's dialect. MSVC has no cpp_std=c++23/c++26, so the
# Windows driver runs this fixture at -Dcpp_std=c++20 -Ddivergent_std=c++latest.
option('divergent_std', type: 'string', value: 'c++26')
