/* Deliberately an empty translation unit: it only has to reach the C++
 * compiler as a non-C++ source. Any instruction here would be
 * architecture-specific, and the object links into prog on every target. */
